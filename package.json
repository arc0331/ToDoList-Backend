{
  "name": "todolist-backend",
  "version": "1.0.0",
  "description": "Backend repository for ToDoList project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx tsc && node dist/index.js",
    "build": "npx tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1398.0",
    "express": "^4.18.2",
    "typescript": "^5.1.3"
  },
  "devDependencies": {
    "@types/node": "^20.3.1"
  }
}
